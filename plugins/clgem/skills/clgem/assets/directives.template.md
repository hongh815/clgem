# Directives & Memory — full text (graphify feed)

<!-- SKILL.md Step 1.6 의 살아있는 지침·기억 전문 저장소. Leader Claude 가 단독 작성.
     이 파일은 graphify 경로 안에 있어 `/graphify <path> --update` 시 각 지침이
     governance 노드로, 지침이 지목한 리소스와 연결된다(authored 엣지 — EXTRACTED 코드 엣지와 구분).
     활성 요약 테이블=Comm.md `## Directives & Memory`, 레지스트리=comm-index.md `## Directives`,
     폐기분 근거=comm-summary.md `## Leader Profile`. 매 항목 일자+시간(시스템 시계) 표기.
     절대 삭제하지 않는다(history 보존): 폐기 시 Status=superseded 로 바꾸고 아래 `## Superseded` 로 옮긴다. -->

## Active

<!-- 카테고리: MUST(지켜야 할 제약) / REMEMBER(기억할 사실) / IMPORTANT(강조된 우선순위) / DESIGN(설계 중점). -->

### U1 — <CATEGORY> — <짧은 제목>

- **Captured**: <YYYY-MM-DD HH:MM TZ> (<사용자 발화 출처>)
- **Category**: <MUST | REMEMBER | IMPORTANT | DESIGN>
- **Directive**: <지침 전문>
- **Governs (resources)**: <이 지침이 적용되는 파일/모듈 — graphify 가 이 이름들로 엣지를 만든다>
- **Status**: active

## Superseded

<!-- 폐기된 지침을 여기로 옮긴다(삭제 금지). `--update` 시 live governance 엣지를 더 내지 않게 한다. -->

<!-- ### U<n> — <CATEGORY> — <제목>  (Status: superseded; superseded <YYYY-MM-DD HH:MM TZ> by <U<m>/사유>) -->
