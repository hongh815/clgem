# Cogem 3.0 Governance Hardening Implementation Plan

**Goal:** Convert the 0.2 policy-only gaps into deterministic 3.0 enforcement while preserving the five-role GPT/Antigravity Plan A topology.

**Architecture:** Add a generic Skill policy module shared by validation and packaging; add graph and review hashes; distinguish project validation from dispatch authorization; extend task and report contracts with review-level and review-proof data; reset the release to an unambiguous bootstrap state.

**Tech Stack:** Python 3.11+ standard library, JSON, Markdown, unittest.

## Tasks

- [x] Add generic `skills/*` frontmatter, layout, manifest, and schema validation.
- [x] Make `manifest.txt` authoritative during archive creation.
- [x] Add graph input fingerprint and stale-graph dispatch blocking.
- [x] Add `dispatch-check`, `skill-check`, and `review-hash` commands.
- [x] Add task `review_level` and structured review proof.
- [x] Enforce task-bound independent and high-risk adversarial review gates.
- [x] Consolidate Skill authoring policy and bootstrap authority.
- [x] Reset live distribution state; packaging and fresh-extraction verification are the release command gate.
